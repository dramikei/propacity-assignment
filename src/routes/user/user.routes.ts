import { Router } from 'express';
const router = Router();

router.get('/me', (req, res) => {
    // Get user details
});

router.post('/signup', (req, res) => {
    // Create user
});

export default router;